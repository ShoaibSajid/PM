# PM - Shoaib Project Management
# Industrial Project Coordination Assistant

# Core dependencies
python>=3.9

# Timezone support (zoneinfo backport for Python < 3.9, pytz as fallback)
pytz>=2023.3
tzdata>=2023.3; platform_system == "Windows"

# Optional: For enhanced text processing
regex>=2023.0.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
